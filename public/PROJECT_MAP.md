# 🗺️ خريطة المشروع - FastShip Shipper App

## 📊 نظرة عامة على البنية

```
fastship-shipper-clean/
├── 📄 README.md              ← الدليل الشامل
├── 📄 QUICK_START.md         ← دليل البدء السريع
├── 📄 PROJECT_MAP.md         ← هذا الملف
├── 📄 .gitignore             ← ملفات Git المستبعدة
├── 📄 index.html             ← الصفحة الرئيسية للموقع
├── 📄 _redirects             ← إعدادات إعادة التوجيه
│
├── 📁 shipper-app/           ← 🚚 التطبيق الرئيسي للشاحن
│   ├── 📁 assets/           
│   ├── 📄 HTML files...      ← صفحات الشاحن
│   
├── 📁 pages/                 ← صفحات عامة ومساعدة
│   ├── 📁 general/          
│   ├── 📁 auth/             
│   └── 📄 profile.html      
│
├── 📁 shared/                ← ملفات مشتركة
│   ├── 📁 css/              
│   └── 📁 js/               
│
├── 📁 config/                ← إعدادات التطبيق
│   └── 📄 supabase.js       
│
└── 📁 database/              ← ملفات قاعدة البيانات
    └── 📄 SQL files...      
```

---

## 🎯 الملفات الرئيسية حسب الوظيفة

### 1️⃣ المصادقة والجلسات
| الملف | الموقع | الوظيفة |
|-------|---------|---------|
| `login.html` | `shipper-app/` | تسجيل دخول الشاحن |
| `register.html` | `shipper-app/` | تسجيل حساب جديد |
| `session-manager.js` | `shipper-app/assets/js/` | إدارة الجلسات |
| `auth-guard.js` | `shipper-app/assets/js/` | حماية الصفحات |
| `auth-manager.js` | `shipper-app/assets/js/` | منطق المصادقة |

### 2️⃣ لوحة التحكم والشحنات
| الملف | الموقع | الوظيفة |
|-------|---------|---------|
| `dashboard.html` | `shipper-app/` | لوحة التحكم الرئيسية |
| `add-shipment.html` | `shipper-app/` | إضافة شحنة جديدة |
| `shipments.html` | `shipper-app/` | قائمة الشحنات |
| `shipper-dashboard.js` | `shipper-app/assets/js/` | منطق لوحة التحكم |
| `shipper-app.js` | `shipper-app/assets/js/` | منطق التطبيق |

### 3️⃣ التواصل والإشعارات
| الملف | الموقع | الوظيفة |
|-------|---------|---------|
| `messages.html` | `shipper-app/` | نظام المراسلة |
| `notifications.html` | `shipper-app/` | التنبيهات |
| `messaging.js` | `shared/js/` | منطق المراسلة |

### 4️⃣ الأنماط والتصميم
| الملف | الموقع | الوظيفة |
|-------|---------|---------|
| `custom.css` | `shipper-app/assets/css/` | أنماط مخصصة |
| `main.css` | `shipper-app/assets/css/` | أنماط رئيسية |
| `professional-fix.css` | `shipper-app/assets/css/` | تصحيحات احترافية |
| `tailwind.min.css` | `shared/css/` | Tailwind CSS |
| `main.css` | `shared/css/` | أنماط مشتركة |

### 5️⃣ قاعدة البيانات والتكوين
| الملف | الموقع | الوظيفة |
|-------|---------|---------|
| `supabase.js` | `config/` | تهيئة Supabase |
| `supabase-config.js` | `config/` | إعدادات إضافية |
| `fastship-schema.sql` | `database/` | المخطط الأساسي |
| `migrate-schema.sql` | `database/` | الترحيل |
| `additional-tables.sql` | `database/` | جداول إضافية |

### 6️⃣ المساعدة والأدوات
| الملف | الموقع | الوظيفة |
|-------|---------|---------|
| `utils.js` | `shared/js/` | دوال مساعدة |
| `matcher.js` | `shared/js/` | المطابقة الذكية |
| `supabase.js` | `shared/js/` | تهيئة Supabase |

---

## 🔗 مخطط العلاقات بين الملفات

### تسلسل تسجيل الدخول
```
login.html
  ↓ يستدعي
session-manager.js
  ↓ يستخدم
config/supabase.js
  ↓ يتصل بـ
Supabase Database
  ↓ يتحقق من
users table
  ↓ في حالة النجاح
dashboard.html
```

### تسلسل إضافة شحنة
```
add-shipment.html
  ↓ Form Submit
shipper-app.js
  ↓ يستخدم
config/supabase.js
  ↓ يُدرج في
shipments table
  ↓ ينتقل إلى
shipments.html
```

### تسلسل المراسلة
```
messages.html
  ↓ يستخدم
messaging.js
  ↓ يستدعي
shared/js/supabase.js
  ↓ يتفاعل مع
messages table
```

---

## 📋 التبعيات

### المكتبات الخارجية
```html
<!-- Supabase -->
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>

<!-- Font Awesome -->
<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">

<!-- Google Fonts -->
<link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@300;400;500;700;900&display=swap" rel="stylesheet">
```

### الملفات المحلية
```html
<!-- CSS -->
<link rel="stylesheet" href="assets/css/custom.css">
<link rel="stylesheet" href="../shared/css/tailwind.min.css">

<!-- JavaScript -->
<script src="../config/supabase.js"></script>
<script src="assets/js/session-manager.js"></script>
```

---

## 🎨 نظام الألوان

### الألوان الرئيسية
```css
--primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
--background: linear-gradient(135deg, #f5f7fa 0%, #e4edf5 100%);
--card-background: white;
--text-primary: #333333;
--text-secondary: #666666;
```

### الحالات
```css
--success: #10b981;
--warning: #f59e0b;
--error: #ef4444;
--info: #3b82f6;
```

---

## 🔐 نظام الحماية

### مستويات الوصول
```
Public (غير محمي)
├── index.html
├── login.html
└── register.html

Protected (محمي)
├── dashboard.html
├── add-shipment.html
├── shipments.html
├── messages.html
└── notifications.html
```

### آلية الحماية
```javascript
1. session-manager.js يتحقق من الجلسة
2. auth-guard.js يحمي الصفحات
3. يتحقق من user_type = 'shipper'
4. إعادة توجيه إلى login.html إذا فشل
```

---

## 📊 جداول قاعدة البيانات

### الجداول الأساسية
1. **users** - معلومات المستخدمين
2. **shipments** - الشحنات
3. **trips** - رحلات الناقلين (للمطابقة)
4. **messages** - الرسائل
5. **notifications** - التنبيهات

### العلاقات
```
users (shipper)
  ↓ has many
shipments
  ↓ can match with
trips
  ↓ belongs to
users (carrier)

users
  ↓ has many
messages (sent/received)

users
  ↓ has many
notifications
```

---

## 🚀 سير العمل

### للمستخدم الجديد
1. يزور `index.html`
2. ينقر على "تسجيل"
3. يملأ `register.html`
4. يتم إنشاء حساب في Supabase
5. إعادة توجيه إلى `login.html`
6. تسجيل الدخول
7. الوصول إلى `dashboard.html`

### للمستخدم الحالي
1. يزور `login.html`
2. يُدخل بيانات الدخول
3. session-manager يتحقق من البيانات
4. إذا صحيحة → `dashboard.html`
5. إذا خاطئة → رسالة خطأ

### لإضافة شحنة
1. من `dashboard.html` → `add-shipment.html`
2. يملأ النموذج
3. إرسال البيانات إلى Supabase
4. إنشاء سجل في جدول shipments
5. إعادة توجيه إلى `shipments.html`

---

## 🔧 نقاط التوسع المستقبلية

### ميزات جاهزة للإضافة
- [ ] نظام تقييم الناقلين
- [ ] تتبع GPS للشحنات
- [ ] نظام الدفع الإلكتروني
- [ ] تقارير وإحصائيات متقدمة
- [ ] تطبيق موبايل

### ملفات جاهزة
```
matcher.js → للمطابقة الذكية المتقدمة
utils.js → يمكن إضافة دوال مساعدة
messaging.js → يمكن تطويره للدردشة الحية
```

---

## 📝 ملاحظات التطوير

### عند إضافة صفحة جديدة
1. أنشئ الملف في `shipper-app/`
2. أضف الحماية (session-manager)
3. استخدم المسارات النسبية الصحيحة
4. أضف التوثيق في هذا الملف

### عند تعديل قاعدة البيانات
1. أنشئ ملف migration جديد
2. ضعه في `database/`
3. نفذه في Supabase
4. حدّث التوثيق

### عند إضافة أنماط
1. إذا خاص بالشاحن → `shipper-app/assets/css/`
2. إذا مشترك → `shared/css/`
3. استخدم نظام الألوان الموحد

---

## 🎯 الخلاصة

هذا المشروع منظم بشكل احترافي ويحتوي على:
- ✅ بنية واضحة ومنطقية
- ✅ فصل تام عن قسم الناقل
- ✅ ملفات مشتركة قابلة لإعادة الاستخدام
- ✅ نظام حماية قوي
- ✅ توثيق شامل

**جاهز للتطوير والتوسع! 🚀**
